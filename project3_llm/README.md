

# 项目3：大模型推荐轻量化部署与RLHF方案设计

## 技术方案

1. 高效训练/推理：LoRA微调 + 4-bit量化 + GGUF压缩 + FastAPI服务，8倍模型压缩（FP32→4-bit）

2. 模型扩展（Scaling）：基座模型可替换为LLaMA-3B/7B，数据千级→百万级

3. RLHF偏好对齐：以用户点击/完播率/停留时长为隐式奖励，通过DPO算法优化生成策略

4. LLM知识对齐：通过LoRA微调实现大模型知识与推荐目标对齐

## 岗位匹配

高效训练/推理 + 强化学习偏好对齐 + LLM知识对齐 → OneRec加分项
